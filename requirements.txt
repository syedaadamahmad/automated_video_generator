# ── Veo Video Generation Platform ────────────────────────────────────────────
# Install: pip install -r requirements_veo.txt
# System:  ffmpeg must be installed separately
#          macOS:  brew install ffmpeg
#          Linux:  apt install ffmpeg

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Google Veo 3.1
google-genai>=1.0.0

# Excel processing
pandas>=2.2.0
openpyxl>=3.1.0       # required by pandas for .xlsx read/write
xlrd>=2.0.1           # required by pandas for legacy .xls read

# Config
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

streamlit
requests

boto3

python-multipart
# ── Optional: only needed if reusing PromptDecomposer from main project ───────
# The Veo orchestrator uses PromptDecomposer which calls AWS Bedrock (Claude Haiku)
# for sub-prompt generation. If you don't have AWS credentials configured,
# it falls back to deterministic sub-prompts automatically — boto3 is not required.
# boto3>=1.34.0
# botocore>=1.34.0